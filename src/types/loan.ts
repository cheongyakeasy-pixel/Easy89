export interface LoanGuide {
  id: string;
  title: string;
  targetUser: string;
  summary: string;
  keyConditions: string[];
  cautionPoints: string[];
  officialUrl: string;
  lastCheckedDate: string;
}
